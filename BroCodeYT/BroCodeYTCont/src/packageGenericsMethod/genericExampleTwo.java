package packageGenericsMethod;

public class genericExampleTwo {
    public static void main(String[] args) {

        // generic scenario: generics benefit can can have one method for all data types

        /*
                Player player = new Player();
        Enemy enemy = new Enemy();
        Item item = new Item();
        Tree tree = new Tree();

        draw(player);
        draw(enemy);
        draw(item);
        draw(tree);
    }
    public static <T> void draw(T x){
        x.draw();
    }



         */

    }
}
